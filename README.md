# 💜 Дельта - Android Приложение

Мобильное приложение с 3D аватаром Дельты и AI чатом для Android.

## ✨ НОВОЕ: AI ДИАЛОГ!

Теперь Дельта может вести полноценный диалог с помощью AI:
- 🧠 Умные ответы на любые вопросы
- 💬 Поддержка контекста разговора
- 💜 Личность из аниме "Восхождение в тени"
- 👑 Обращается "Босс" как в аниме
- 🎭 Эмоции и манера речи Дельты

## 📱 Возможности

- ✅ 3D аватар Дельты (анимированный)
- ✅ Текстовый чат с AI (полноценный диалог!)
- ✅ Красивый фиолетовый дизайн в стиле Telegram
- ✅ Работает офлайн (базовые ответы)
- ✅ Работает онлайн (AI диалог с Ollama)
- ✅ Легкое и быстрое

## 🛠️ Сборка APK

### Вариант 1: Linux (Рекомендуется)

```bash
# Установка зависимостей
sudo apt update
sudo apt install -y python3-pip build-essential git zip unzip
sudo apt install -y openjdk-11-jdk
sudo apt install -y autoconf libtool pkg-config zlib1g-dev
sudo apt install -y libncurses5-dev libncursesw5-dev libtinfo5
sudo apt install -y libffi-dev libssl-dev

# Установка Buildozer
pip3 install buildozer cython

# Сборка APK
cd android_app
buildozer android debug

# APK будет в: bin/deltaapp-0.1-debug.apk
```

### Вариант 2: Google Colab (Онлайн)

1. Открой [Google Colab](https://colab.research.google.com)
2. Создай новый блокнот
3. Вставь код:

```python
# Установка зависимостей
!apt update
!apt install -y openjdk-11-jdk
!pip install buildozer cython

# Загрузи файлы проекта через интерфейс
# Или клонируй из GitHub

# Сборка APK
!cd android_app && buildozer android debug

# Скачай APK
from google.colab import files
files.download('android_app/bin/deltaapp-0.1-debug.apk')
```

### Вариант 3: Windows (через WSL)

```bash
# Установи WSL
wsl --install

# Открой Ubuntu в WSL
# Следуй инструкциям из Варианта 1
```

## 📥 Установка на телефон

1. Скачай APK файл
2. Открой на телефоне
3. Разреши установку из неизвестных источников
4. Установи приложение
5. Готово! 💜

## ⏱️ Время сборки

- Первая сборка: 20-40 минут (скачивает Android SDK)
- Последующие: 5-10 минут

## 📦 Размер APK

- Debug версия: ~20-30 МБ
- Release версия: ~15-20 МБ (после оптимизации)

## 🎨 Настройка

Отредактируй `buildozer.spec` для изменения:
- Названия приложения
- Иконки
- Разрешений
- Версии

## 🚀 Запуск на компьютере (для тестирования)

```bash
# Установи Kivy
pip install kivy

# Запусти приложение
python main.py
```

## 📱 Отправка через Telegram бот

После сборки APK, обнови Telegram бота:

```python
# В telegram_cluster_bot.py
@bot.message_handler(commands=['app'])
def app_command(message):
    app_file = "android_app/bin/deltaapp-0.1-debug.apk"
    
    with open(app_file, 'rb') as file:
        bot.send_document(
            message.chat.id,
            file,
            caption="💜 Дельта - Android приложение\n\n📱 Установи на телефон!"
        )
```

## ❓ Частые проблемы

**Q: Buildozer не работает на Windows**  
A: Используй WSL или Google Colab

**Q: Сборка долго идёт**  
A: Первая сборка скачивает Android SDK (2-3 ГБ), это нормально

**Q: APK не устанавливается**  
A: Разреши установку из неизвестных источников в настройках Android

**Q: Приложение вылетает**  
A: Проверь логи: `adb logcat | grep python`

## 🔥 Дальнейшие улучшения

- [ ] Голосовой ввод/вывод
- [ ] Push-уведомления
- [ ] Работа в фоне
- [ ] Виджет на главный экран
- [ ] Интеграция с системой
- [ ] Публикация в Google Play

## 📄 Лицензия

MIT License

## 💜 Автор

Создано с любовью для фанатов "Восхождения в тени"!
